package com.example.upipayment.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class SwaggerConfig {
    // Add springdoc-openapi dependency later if Swagger UI is required.
}
